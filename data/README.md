# Private data directory

Place the approved, non-public research inputs here:

```text
cb_ch_all.xlsx
cb_ch_call_ver1.1.xlsx
Prehospital_final_model.pkl
```

The directory is excluded from Git except for this file. Never commit clinical
records, identifiers, emergency-call audio or transcripts, slot-filling
outputs, or the frozen prediction model.
